[INPUT]
    Name tail
    Path /var/log/containers/*.log
    Tag kube.*
    Parser json
    Multiline.Parser docker

[FILTER]
    Name kubernetes
    Match kube.*

[FILTER]
    Name lua
    Match kube.*
    script enrich.lua
    call add_context

[OUTPUT]
    Name opentelemetry
    Host otel-collector
